Install OSSEC Server (UBUNTU Desktop)

# Add Apt sources.lst 
wget -q -O - https://updates.atomicorp.com/installers/atomic | sudo bash 
 
 

# Update apt data 
sudo apt-get update 
 
 

# Server 
sudo apt-get install ossec-hids-server 


git init
sudo nano file.txt
git add
git commit
git remote add origin https://github.com/migmiguel/MITT-JUAN-MIGUEL-AL-EXAM-OCTOBER-2018.git
git push origin master 
